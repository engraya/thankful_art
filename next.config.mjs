/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'randomuser.me', 'unsplash.com', 'pagedone.io'],
      },
};

export default nextConfig;
